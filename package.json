{
  "name": "hosinoneko-sub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "echo 'Deploy via Git push to Cloudflare Pages'",
    "db:init": "wrangler d1 execute nct-subscribe --file=./schema.sql",
    "db:migrate": "wrangler d1 migrations apply nct-subscribe"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
